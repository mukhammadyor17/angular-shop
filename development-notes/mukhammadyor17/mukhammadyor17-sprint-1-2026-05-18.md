# Sprint 1 — Development Notes

**Author:** mukhammadyor17 | **Date:** 2026-05-18

- **What was done:** Styled the `BaseCard` component (padding, border-radius, white background, subtle shadow). Created the `ProfileCard` component with avatar and user info layout. Added a `ProfilePage` and registered its route. Migrated all `href` links in `PageHeader` to Angular `routerLink` directives with active state support.
- **Problems:** Was not immediately clear how `routerLinkActive` and `RouterLinkActiveOptions` need both the directive import and the decorator `imports` array updated — the template error only showed up after adding the directive.
- **Solutions:** Added `RouterLink` and `RouterLinkActive` to both the `imports` array in the `@Component` decorator and the TypeScript import statement.
- **What I learned:** In Angular standalone components, every directive used in the template must be explicitly listed in the component's `imports` array — there is no module to inherit from. `[routerLinkActiveOptions]="{ exact: true }"` is needed for the home route to avoid it being matched as active on every page.
- **Plans:** In Sprint 2, build out the catalog page with product cards and connect a products service using Angular signals.
- **Time spent:** 3 hours

**!!!In the last 3 commits, I used AI to create the basic UI. The final commit texts and the last note were also generated by AI.**
