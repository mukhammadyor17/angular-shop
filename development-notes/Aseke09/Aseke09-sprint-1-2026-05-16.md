## Sprint 1 - development notes

#### Aseke09 | for 2026-05-16 (lazy coders team)
## What I did
- Updated application routing in `app.routes.ts`
- Continued developing the `catalog-page`
- Created a reusable `ProductCard` standalone component
- Added component communication using:
  - `input.required()`
  - `output()`
- Practiced parent → child and child → parent communication
- Worked with Angular bindings:
  - interpolation `{{ }}`
  - property binding `[src]`
  - event binding `(click)`
- Added product rendering logic and "Add to cart" event emitting

I created a reusable ProductCard standalone component and practiced component communication using:

input.required()
output()
event emitting
parent → child communication
child → parent communication

I also worked with Angular bindings:

interpolation {{ }}
property binding [src]
event binding (click)

Additionally, I added product rendering logic and implemented "Add to cart" event emitting.

I also explored Angular lifecycle hooks:

ngOnInit
ngOnDestroy

## Problems
At first, I did not fully understand the difference between interpolation and property binding.

I was also confused about:

when to use input() / output()
when shared services are more appropriate
how Angular component communication differs from React

While working on styles, I also had some difficulties organizing reusable global SCSS architecture and separating tokens, layouts, and utility styles properly.

## Solution / Attempts

I realized:

- interpolation is mainly for displaying values
- property binding updates DOM properties dynamically
- output() is used for child-to-parent communication
reusable standalone components simplify project structure

## Whai I Learned
- Better understanding of Angular standalone components
- Improved knowledge of Angular component communication
- Learned practical usage of:
    input.required()
    output()
    event emitters
- Better understanding of Angular bindings
- Learned how Angular lifecycle hooks work

## Plans for Sprint 2
- learn Angular services
- understand dependency injection better
- start routing deeper
- improve cart functionality
- connect components with backend/API data
- continue building reusable UI components
- improve responsive styling

## AI Usage

- Used AI for explaining Angular component communication
- Used AI for understanding Angular bindings and lifecycle hooks
- Used AI for recommendations on SCSS architecture and file organization
- Wrote manually all component logic, routing updates, styling, and rendering logic
- Checked manually all routes, bindings, styles, and component interactions

## Time spent 

4 hours