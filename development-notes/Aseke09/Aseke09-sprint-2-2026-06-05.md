## Sprint 2 - development notes

#### Askhat Tassybayev | for 2026-06-05 (lazy coders team)
## What I did

* Started implementing the Catalog and Cart features in the Angular application.
* Connected the frontend to the team's NestJS backend instead of using mock product data.
* Studied the products API and analyzed the backend response structure.
* Created product-related models and added a dedicated `ProductsResponse` model for API responses.
* Implemented `CatalogService` using `HttpClient` and environment configuration.
* Added product loading from the backend and integrated it with Angular Signals.
* Implemented `CartService` with signal-based state management.
* Worked on product card integration and communication between Catalog and Cart features.
* Reviewed the application routing configuration and page navigation.

## Problems

* The frontend product model did not match the backend response structure.
* TypeScript errors occurred because the API returned paginated data (`data` and `meta`) instead of a plain array.
* Product card types were inconsistent with the backend product model.
* While testing navigation, I discovered that the Cart link redirects to the Profile page instead of the Cart page.

## Solutions

* Analyzed the backend API response and adjusted the frontend models accordingly.
* Created a separate `ProductsResponse` interface to properly type API responses.
* Updated services to work with typed backend responses and extract product data correctly.
* Refactored product-related components to use a shared Product model.
* Investigated routing issues and identified the incorrect Cart navigation behavior for future fixes.

## What I learned

* How to consume a NestJS API from Angular using `HttpClient`.
* How to model paginated API responses with TypeScript interfaces.
* How Angular Signals can be used for component and service state management.
* How to organize feature-based architecture with models, services, and pages.
* How Angular routing and navigation are connected to page rendering.

## AI usage

* Used AI to understand Angular Signals and service architecture.
* Used AI to debug TypeScript typing issues and API integration problems.
* Used AI to design frontend models based on backend responses.
* Used AI to troubleshoot routing and component communication issues.

## Plans for Sprint 3

* Finish Catalog integration with backend data.
* Implement product details page using route parameters and API requests.
* Improve Cart functionality and product management.
* Add search, filtering, sorting, and pagination support in the Catalog.
