export interface User {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  photoUrl: string | null;
  dateOfBirth: string;
  role: string;
  createdAt: string;
  updatedAt: string;
}
