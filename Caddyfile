:3000 {
  # Proxy API requests to the backend
  handle /api/* {
    reverse_proxy ecommerce-api-free:3000
  }

  # Serve static files
  handle {
    root * /app/browser/browser
    try_files {path} {path}/ /index.html
    file_server
  }
}
